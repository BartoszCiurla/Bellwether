<Page
    x:Class="Bellwether.Views.JokePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    mc:Ignorable="d">
    <!--<Page.Resources>
        <DataTemplate x:Key="Jo" x:DataType="viewModels:JokeViewModel">
            <Grid Width="280">
                <TextBlock Text="{x:Bind JokeContent}" 
                   Margin="8,0,0,0" HorizontalAlignment="Left" TextWrapping="Wrap"/>
            </Grid>
        </DataTemplate>
        
    </Page.Resources>
    <Grid>
        <ListView x:Name="Jokes" ItemsSource="{Binding Jokes}"          
              ItemTemplate="{StaticResource Jo}"
                  SelectionChanged="{Binding SelectedJoke, Mode=TwoWay}"
                          Height="400">
    </ListView>
    </Grid>-->
</Page>
