<Page
    x:Class="Bellwether.Views.OptionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bellwether.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel>
        <ComboBox ItemsSource="{Binding Languages}" SelectedItem="{Binding SelectedLanguage, Mode=TwoWay}" DisplayMemberPath="LanguageName" PlaceholderText="Dupa zbita" Padding="12" BorderThickness="0" HorizontalAlignment="Stretch"></ComboBox>
        <Button Command="{Binding ChangeLanguageCommand}" Padding="12" BorderThickness="0" Margin="12 8" HorizontalAlignment="Stretch" Content="Zmień język" Foreground="White" FontWeight="Bold"></Button>
            <TextBlock Text="{Binding CurrentLanguage.LanguageName}"  FontWeight="Light" Margin="12"/>
        </StackPanel>
    </Grid>
</Page>
